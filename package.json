{
  "name": "fincommerce",
  "version": "0.1.0",
  "private": true,
  "description": "Enterprise e-commerce and payment simulation platform using NestJS microservices.",
  "packageManager": "npm",
  "workspaces": [
    "services/*",
    "libs/*"
  ],
  "scripts": {
    "build": "npm run build -w @fincommerce/common && npm run build -w auth-service",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
