{
  "name": "fincommerce",
  "version": "0.1.0",
  "private": true,
  "description": "Enterprise e-commerce and payment simulation platform using NestJS microservices.",
  "packageManager": "npm",
  "workspaces": [
    "services/*",
    "libs/*"
  ],
  "scripts": {
    "prisma:generate": "npm run prisma:generate -w auth-service && npm run prisma:generate -w user-service && npm run prisma:generate -w @fincommerce/seller-service",
    "build": "npm run prisma:generate && npm run build -w @fincommerce/common && npm run build -w auth-service && npm run build -w user-service && npm run build -w @fincommerce/seller-service",
    "test": "npm run prisma:generate && npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
